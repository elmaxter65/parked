name: parkedrenovated
description: system parked
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  rxdart: ^0.23.1
  equatable: ^0.2.6
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true